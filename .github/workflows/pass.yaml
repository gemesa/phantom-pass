name: Pass

on: [push]

jobs:
  pass:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install deps
        run: sudo apt update && sudo apt install llvm libboost-all-dev libssl-dev -y

      - name: Build and run passes
        run: make

      - name: Run the executables
        run: make run
